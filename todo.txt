Lista de melhorias:
        -Melhorar busca por linguagem natural utilizando tambem os filtros, atualmente ele realiza a busca em linguagem natural utilizando apenas os parametros desejados no texto.
        -O prompt que envolve idade, precisa falar a idade minima e maxima para funcionar corretamente. Se vc falar "pacientes com diabetes ate 60 anos" ele nao vai funcionar corretamente, precisa falar "pacientes com diabetes entre 0 e 60 anos" ]
        -Fazer com que nao precise imprimir o codigo na tela para gerar o grafico, deixar apenas o botao "Gerar gráfico" 
        -gerar CSV, JSON, , ate mesmo HTML ETC...
        -fazer lista de perguntas padrao, criando um log de perguntas anteriores feitas. 